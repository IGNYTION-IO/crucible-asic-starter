# UART - 01 RTL

Synthesizable Verilog source. Top module: `uart_core` in `uart_core.v`.
One module per file, file name matches module name.

**Output artifact:** `uart_core.v` (and any submodules it instantiates)
