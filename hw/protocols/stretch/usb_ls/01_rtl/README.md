# USB low-speed (stretch) - 01 RTL

Synthesizable Verilog source. Top module: `usb_ls_core` in `usb_ls_core.v`.
One module per file, file name matches module name.

**Output artifact:** `usb_ls_core.v` (and any submodules it instantiates)
