# 10BASE-T Ethernet (stretch) - 01 RTL

Synthesizable Verilog source. Top module: `ethernet_core` in `ethernet_core.v`.
One module per file, file name matches module name.

**Output artifact:** `ethernet_core.v` (and any submodules it instantiates)
