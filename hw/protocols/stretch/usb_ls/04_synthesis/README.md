# USB low-speed (stretch) - 04 Synthesis

Yosys synthesis of `usb_ls_core` against the IHP SG13G2 standard cell library
(`lib/`, not yet populated - see `tapeout/README.md`), driven by `synth.tcl`.

**Output artifacts:** `synth_output.v`, `constraints.sdc`, area/cell report
