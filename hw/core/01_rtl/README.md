# Core (full chip) - 01 RTL

Synthesizable Verilog source. Top module: `protocol_emulator_top` in `protocol_emulator_top.v`.
One module per file, file name matches module name.

**Output artifact:** `protocol_emulator_top.v` (and any submodules it instantiates)
