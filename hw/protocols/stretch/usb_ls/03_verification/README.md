# USB low-speed (stretch) - 03 Verification

cocotb testbench and self-checking tests in `py_verification_usb_ls_core/`. Constrained-random and coverage results land in `py_verification_usb_ls_core/results/`.

**Output artifacts:** `py_verification_usb_ls_core/results/*.xml`, coverage report
