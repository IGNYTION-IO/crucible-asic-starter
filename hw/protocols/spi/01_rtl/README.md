# SPI - 01 RTL

Synthesizable Verilog source. Top module: `spi_core` in `spi_core.v`.
One module per file, file name matches module name.

**Output artifact:** `spi_core.v` (and any submodules it instantiates)
